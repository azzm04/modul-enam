# Copy to .env and set the same value in backend/.env
SENSOR_API_KEY=change_this_to_a_random_secret
BACKEND_BASE_URL=http://localhost:5000
# Optional: Expo push token for notifications
EXPO_PUSH_TOKEN=ExponentPushToken[YOUR_TOKEN_HERE]
